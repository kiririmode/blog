# =============================================================================
# ブログ管理システム環境変数設定ファイル
# =============================================================================
# このファイルは環境変数の設定例です。実際の値は .env ファイルに設定してください。
# .env ファイルは .gitignore に含まれており、バージョン管理されません。

# -----------------------------------------------------------------------------
# Perplexity AI API設定
# -----------------------------------------------------------------------------
# Perplexity AIのAPIキー
# 取得方法: https://www.perplexity.ai/settings/api
PERPLEXITY_API_KEY=your_perplexity_api_key_here

# -----------------------------------------------------------------------------
# GitHub連携設定
# -----------------------------------------------------------------------------
# GitHubのPersonal Access Token
# 取得方法: https://github.com/settings/tokens
# 必要な権限: repo (リポジトリへのフルアクセス)
GITHUB_PERSONAL_ACCESS_TOKEN=your_github_personal_access_token_here

# -----------------------------------------------------------------------------
# AWS設定
# -----------------------------------------------------------------------------
# AWSアクセスキーID
# 画像アップロードやその他のAWSサービス利用に必要
# 取得方法: AWS IAMコンソール > セキュリティ認証情報
AWS_ACCESS_KEY_ID=your_aws_access_key_id_here

# AWSシークレットアクセスキー
# アクセスキーIDと対になるシークレットキー
AWS_SECRET_ACCESS_KEY=your_aws_secret_access_key_here

# AWSリージョン
# 使用するAWSリージョンを指定（例: ap-northeast-1は東京リージョン）
AWS_REGION=ap-northeast-1

# -----------------------------------------------------------------------------
# Claude Code テレメトリー設定
# -----------------------------------------------------------------------------
# Claude Codeの使用状況データ収集の有効/無効
# 1: 有効, 0: 無効
CLAUDE_CODE_ENABLE_TELEMETRY=1

# -----------------------------------------------------------------------------
# OpenTelemetry設定
# -----------------------------------------------------------------------------
# メトリクスのエクスポート形式
# otlp: OpenTelemetry Protocol形式でエクスポート
OTEL_METRICS_EXPORTER=otlp

# OTELエクスポーターのプロトコル
# http/protobuf: HTTP経由でProtocol Buffers形式を使用
OTEL_EXPORTER_OTLP_PROTOCOL=http/protobuf

# OTELエクスポーターのエンドポイントURL
# テレメトリーデータを送信する先のURL
OTEL_EXPORTER_OTLP_ENDPOINT=https://otel.kiririmo.de

# セッションIDをメトリクスに含めるかどうか
OTEL_METRICS_INCLUDE_SESSION_ID=true

# バージョン情報をメトリクスに含めるかどうか
OTEL_METRICS_INCLUDE_VERSION=true

# アカウントUUIDをメトリクスに含めるかどうか
OTEL_METRICS_INCLUDE_ACCOUNT_UUID=true

# リソース属性の追加設定
# メトリクスに付与する追加の属性（例: ユーザー識別など）
OTEL_RESOURCE_ATTRIBUTES="who=your_identifier_here"

# -----------------------------------------------------------------------------
# Obsidian連携設定
# -----------------------------------------------------------------------------
# Obsidian Local REST APIのAPIキー
# Obsidianとの連携に必要（Obsidian Local REST APIプラグインから取得）
OBSIDIAN_API_KEY="your_obsidian_api_key_here"
NODE_TLS_REJECT_UNAUTHORIZED=0
# -----------------------------------------------------------------------------
# はてなブログ連携設定（blogsync用）
# -----------------------------------------------------------------------------
# これらの環境変数は npm run prepare-publish 実行時に必要です

# はてなID（ユーザー名）
# 例: kiririmode
# HATENA_USERNAME=your_hatena_username_here

# はてなブログのAtomPub APIキー
# 取得方法: https://blog.hatena.ne.jp/my/config の「APIキー」セクション
# HATENA_API_KEY=your_hatena_api_key_here

# blogsync用のはてなID（通常はHATENA_USERNAMEと同じ）
# BLOGSYNC_USERNAME=your_hatena_username_here

# blogsync用のパスワード（通常はHATENA_API_KEYと同じ）
# BLOGSYNC_PASSWORD=your_hatena_api_key_here
